<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cindodcindy.nitippembeli">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NitipPembeli">
        <activity android:name=".view.InputPaymentActivity"></activity>
        <activity android:name=".view.InputBookingActivity" />
        <activity android:name=".view.DoneDetailActivity" />
        <activity android:name=".view.PaymentDetailActivity" />
        <activity android:name=".view.KonfirmDetailActivity" />
        <activity android:name=".view.BookingDetailActivity" />
        <activity android:name=".view.BottomNavActivity" />
        <activity android:name=".view.LoginActivity" />
        <activity android:name=".view.RegisActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>